#ifndef __N_FONC__
#define __N_FONC__

#include <stdio.h>
#include "uses_fonc.h"
#include "Types.h"

int graphcolor(GRAPH graph, int k, int p); //retourne 1 si la colo est bonne, 0 sinon

int checkcolo(GRAPH graph); //retourne 1 si la colo est bonne, 0 sinon

#endif
