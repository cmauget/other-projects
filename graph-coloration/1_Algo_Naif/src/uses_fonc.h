//copy de uses_fonc pour le makefile

#ifndef __UFONC__
#define __UNFONC__

#include <stdio.h>
#include <stdlib.h>
#include "Types.h"

GRAPH initgraph(int n); //initialise le graph

void affichegraph(GRAPH graph); //affiche le graph

void affichecolor(GRAPH graph); // affiche les couleurs


#endif
